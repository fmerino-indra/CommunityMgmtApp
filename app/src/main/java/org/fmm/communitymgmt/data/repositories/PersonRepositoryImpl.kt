package org.fmm.communitymgmt.data.repositories

class PersonRepositoryImpl {

/*
   override suspend fun getPerson(id: Int): Person {
        // Obtener datos de la API
        val response = apiService.getPerson(id)
        // Mapeo de datos
        return Person(id = response.id, name = response.name)
    }

 */

}